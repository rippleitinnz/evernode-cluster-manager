{
  "name": "evernode-cluster-manager-contract",
  "version": "1.0.0",
  "dependencies": {
    "evernode-client-cluster-manager": "file:/home/chris/evernode-client-cluster-manager-pkg",
    "hotpocket-nodejs-contract": "0.7.4"
  },
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp src/index.js dist/index.js && cp -r node_modules dist/node_modules && cp package.json dist/package.json && rm -rf dist/node_modules/evernode-client-cluster-manager && cp -r /home/chris/evernode-client-cluster-manager-pkg/dist dist/node_modules/evernode-client-cluster-manager && cp /home/chris/evernode-client-cluster-manager-pkg/package.json dist/node_modules/evernode-client-cluster-manager/package.json"
  }
}
